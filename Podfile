# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RSScoreDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!
  pod "Appirater"
  # Pods for RSScoreDemo

  target 'RSScoreDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RSScoreDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
